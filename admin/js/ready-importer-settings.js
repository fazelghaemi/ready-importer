/**
 * فایل جاوااسکریپت بخش تنظیمات Ready Importer
 *
 * --- جدید (فاز ۲) ---
 *
 * این فایل مسئولیت مدیریت UI صفحه تنظیمات را بر عهده دارد
 * (مخصوصاً برای فیلدهای تکرارشونده "Rules").
 */
(function($) {
    'use strict';

    $(document).ready(function() {
        
        console.log('Ready Importer Settings JS Loaded.');

        // در آینده، منطق مربوط به اضافه/حذف کردن
        // فیلدهای تکرارشونده (Price Rules و Find/Replace)
        // در اینجا نوشته خواهد شد.

    }); // انتهای document.ready

})(jQuery);